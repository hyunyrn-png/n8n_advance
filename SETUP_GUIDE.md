# 펀드 & 금융정책 모닝 브리핑 (개선판) - 설정 가이드

## 개요

기존 워크플로우를 복제하여 아래 기능을 추가/개선한 버전입니다.
**기존 워크플로우는 그대로 유지되며, 새 워크플로우는 별도로 동작합니다.**

## 변경 사항 요약

### 1. 정부정책 (금융위원회)
- **금융위원회 보도자료** (`https://www.fsc.go.kr/no010101`) 당일 등록 건 수집
- 첨부된 PDF 파일 자동 다운로드
- 구글시트 `정부정책` 시트에 별도 저장
- `정부정책_발송내역` 시트로 중복 발송 방지

### 2. 뉴스 수집 개선
- 네이버 뉴스: **전날 06:30 ~ 당일 06:30** 시간 범위 필터링
- 검색 키워드 확대: `펀드`, `공모펀드`, `ETF 자산운용`, `금융투자 펀드`
- 수집 결과 수: 5건 → **50건**으로 확대
- 구글시트 `네이버뉴스_수집` 시트에 별도 저장
- 구글뉴스 RSS와 **중복 제거** 후 발송

### 3. 신규 공모펀드 (KOFIA)
- **KOFIA 전자공시** (`dis.kofia.or.kr`) 조회
- 조회 기간: 지난주 월요일 ~ 일요일
- 공모펀드만 필터링
- 저장 항목: 회사, 펀드명, 설정일, 펀드유형, 투자지역, 표준코드
- **간이투자설명서** 조회 후 투자전략 자동 추출
- 구글시트 `신규펀드` 시트에 저장

### 4. 발송 형식 개선
기존 발송 규칙(제목, 중요도, 이유, 영향) 유지하면서 3개 섹션으로 구분:

```
📊 2026년 3월 16일 월요일
펀드 & 금융정책 모닝 브리핑
━━━━━━━━━━━━━━━━━━━━

📋 정부시책
━━━━━━━━━━━━━━━━━━━━
[금융위 정책 내용 - 제목, 중요도, 이유, 영향]

📰 주요 뉴스
━━━━━━━━━━━━━━━━━━━━
1️⃣ 뉴스 제목 : ... (노출 빈도수: N회)
중요도 : ★★★★☆
이유 : ...
영향 : ...
URL : ...

🆕 신규 공모펀드
━━━━━━━━━━━━━━━━━━━━
[펀드 목록 및 투자전략]

━━━━━━━━━━━━━━━━━━━━
⏰ 자동 발송
```

## 구글시트 사전 설정

기존 구글시트 (`일일브리핑`)에 아래 시트를 추가해야 합니다:

### 시트 1: `정부정책`
| 날짜 | 제목 | 내용요약 | 원문링크 | PDF파일수 | 발송여부 | 등록일시 |
|------|------|----------|----------|-----------|----------|----------|

### 시트 2: `정부정책_발송내역`
| 제목 | 발송일시 | 채널 |
|------|----------|------|

### 시트 3: `네이버뉴스_수집`
| 제목 | 링크 | 발행일 | 수집일시 |
|------|------|--------|----------|

### 시트 4: `신규펀드`
| 회사 | 펀드명 | 설정일 | 펀드유형 | 투자지역 | 표준코드 | 투자전략 | 등록일시 |
|------|--------|--------|----------|----------|----------|----------|----------|

## 설치 방법

1. n8n 관리 화면에서 **Import workflow** 실행
2. `workflows/enhanced_morning_briefing.json` 파일 업로드
3. 구글시트에 위 4개 시트 생성
4. Credential 연결 확인 (기존 것 재사용):
   - Google Sheets OAuth2
   - Gmail OAuth2
   - Telegram API
5. **API 키 설정** (보안상 플레이스홀더로 되어 있음):
   - `Claude AI 분석` 노드의 `ANTHROPIC_API_KEY` → n8n 환경변수 또는 직접 입력
   - `네이버 뉴스 API` 노드의 `X-Naver-Client-Id` / `X-Naver-Client-Secret` → 기존 값 입력
6. 워크플로우 **Active** 전환

## 주의사항

- 기존 워크플로우(`펀드 & 금융정책 모닝 브리핑`)는 **비활성화** 후 새 워크플로우를 활성화하세요
- 새 워크플로우는 `active: false` 상태로 임포트됩니다
- KOFIA API는 XML 기반으로, 사이트 구조 변경 시 파싱 로직 수정이 필요할 수 있습니다
- 금융위원회 사이트의 HTML 구조가 변경될 경우 FSC 파싱 노드 수정이 필요합니다

## 워크플로우 흐름도

```
매일 아침 7시
├── 금융위원회 보도자료 → 파싱 → PDF 다운로드 → 구글시트 저장 → 중복확인 ──┐
├── 기획재정부 RSS ──┐                                                      │
├── 구글뉴스 RSS ────┤→ 뉴스 병합 → 필터링/중복제거 ─────────────────────────┤
├── 네이버 뉴스 → 시간필터링 → 구글시트 저장 → 필터링/중복제거 ──────────────┤
├── KOFIA 신규펀드 → 파싱 → 상세조회 → 투자전략 추출 → 구글시트 저장 ───────┤
└── 과거이력조회 ──────────────────────────────────────────────────────────────┤
                                                                              ↓
                                                              전체 데이터 통합
                                                                   ↓
                                                           Claude AI 분석
                                                                   ↓
                                               ┌────────┬──────────┼──────────┐
                                               ↓        ↓          ↓          ↓
                                          텔레그램   Gmail    구글시트    정부정책
                                            발송     발송    발송이력    발송내역
```
